let
    class A (a:bool, b:bool){

        fun getBool:bool (c:bool)
            let
            var r:bool = a && b && c;
            in r;
    }

    var bl:A = new A(true,true);
in
    print(bl.getBool(true));
